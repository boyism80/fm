"use strict";

const EQUIP_SLOT = Object.freeze({
    TOP: -5,
    BOTTOM: -6,
    SHOES: -7,
    WEAPON: -11,
});

const LOOK_SLOT = Object.freeze({
    TOP: 5,
    BOTTOM: 6,
    SHOES: 7,
    WEAPON: 11,
});

module.exports = {
    EQUIP_SLOT,
    LOOK_SLOT,
};
